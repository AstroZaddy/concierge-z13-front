/**
 * Shared constants used across the application
 */

// API base URL - relative path that works through Caddy proxy
export const API_BASE_URL = "/api";

// LocalStorage key for zodiac mode preference
export const STORAGE_KEY = "z13-zodiac-mode";

